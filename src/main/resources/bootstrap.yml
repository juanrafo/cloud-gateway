spring:
  application:
    name: cloud-gateway
  cloud:
    config:
      uri: http://host.docker.internal:9000
      profile: dev
      enabled: true
  main:
    allow-bean-definition-overriding: true

logging:
  level:
    org:
      springframework:
        cloud:
          gateway: DEBUG